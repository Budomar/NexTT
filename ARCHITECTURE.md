# Архитектура RadiaTool

## Обзор проекта
RadiaTool - программа для подбора аналогов радиаторов METEOR с автоматическим распознаванием параметров из спецификаций конкурентов.

## Структура файлов

### Основные модули

#### 🏠 `start_v10.204.py` - ГЛАВНЫЙ ФАЙЛ
**Назначение:** Основной запускаемый файл приложения
**Содержит:**
- Класс `RadiatorApp` - главное окно приложения
- GUI интерфейс (tkinter)
- Координация работы всех модулей
- Обработчики событий пользователя

#### 📄 `pdf_parser.py` - УНИВЕРСАЛЬНЫЙ ПАРСЕР PDF
**Назначение:** Автоматическое извлечение данных из PDF-спецификаций с максимальной отладкой
**Ключевые классы:**
- `PDFParser` - универсальный парсер PDF с поддержкой различных стратегий
**Основные функции:**
- Многостратегический парсинг таблиц (текст, линии, гибридные методы)
- Автоматическое распознавание радиаторов из текста
- Исправление OCR-артефактов и нормализация текста
- Пакетная обработка директорий с PDF
- Детальное логирование и отладка
- Генерация Excel-отчетов с анализом результатов
**Особенности:**
- Поддержка форматов: Royal Thermo, EVRA, PUPMO и других
- Нормализация кириллицы и латиницы
- Удаление дублирующихся символов (OCR-артефакты)
- Автоматическое определение структуры колонок

#### 🎯 `pattern_manager.py` - МЕНЕДЖЕР ШАБЛОНОВ
**Назначение:** Управление шаблонами для автоматического распознавания радиаторов
**Ключевые классы:**
- `PatternManager` - загрузка, сохранение и поиск шаблонов
**Основные функции:**
- Работа с файлом `patterns.json`
- Регулярные выражения для распознавания названий
- Обучение новым шаблонам
- Приоритетная система совпадений

#### 🔍 `parsers.py` - ПАРСЕРЫ НАЗВАНИЙ
**Назначение:** Извлечение параметров из названий радиаторов
**Ключевые классы:**
- `RadiatorNameParser` - универсальный парсер
**Поддерживаемые бренды:**
- METEOR, LIDEA, EVRA, PRADO, OASIS, KERMI
**Форматы:**
- "тип\высота\длина", "тип-высота-длина", специфичные форматы брендов
**Основные методы:**
- `advanced_parse_radiator_name()` - универсальный парсер с приоритетом форматов
- `parse_evra_name()` - специализированный для EVRA
- `parse_foreign_radiator_name_flexibly()` - для иностранных брендов
- Логика определения подключения и конвертации параметров

#### 🏗️ `interface_builder.py` - ПОСТРОИТЕЛЬ ИНТЕРФЕЙСА
**Назначение:** Создание и управление графическим интерфейсом приложения
**Ключевые классы:**
- `InterfaceBuilder` - построение всех элементов GUI
**Основные функции:**
- Создание главного интерфейса с матрицей радиаторов
- Настройка стилей и темы приложения
- Построение системы прокрутки и компоновки
- Создание контекстных меню и подсказок
- Управление матрицей радиаторов с динамическим обновлением
**Компоненты интерфейса:**
- Верхняя панель с меню
- Блок выбора типа подключения с визуальными подсказками
- Матрица радиаторов с размерами высота/длина
- Блок кронштейнов и параметров
- Система скидок
- Нижняя панель управления

#### 📊 `correspondence_manager.py` - ТАБЛИЦА СООТВЕТСТВИЙ
**Назначение:** Управление интерфейсом сопоставления радиаторов
**Ключевые классы:**
- `CorrespondenceManager` - окно таблицы соответствий
**Функции:**
- Отображение загруженных данных
- Ручной и автоматический подбор аналогов
- Перенос данных в основную матрицу

#### 🎲 `meteor_selector.py` - ВЫБОР АНАЛОГОВ METEOR
**Назначение:** Интерфейс выбора подходящих аналогов
**Ключевые классы:**
- `MeteorSelector` - окно выбора аналогов
**Функции:**
- Фильтрация по параметрам (тип, высота, длина)
- Визуальный выбор подключения
- Интеграция с обучением шаблонов

#### 🗂️ `column_selector.py` - ВЫБОР СТОЛБЦОВ
**Назначение:** Интерфейс выбора столбцов в загружаемых файлах
**Ключевые классы:**
- `ColumnSelector` - выбор столбцов наименования и количества
**Функции:**
- Предпросмотр данных
- Автоподбор столбцов
- Подтверждение выбора

#### 📑 `pdf_page_selector.py` - ВЫБОР СТРАНИЦ PDF
**Назначение:** Интерактивный интерфейс для выбора страниц PDF при обработке многостраничных документов
**Ключевые классы:**
- `PdfPageSelector` - основное окно выбора страниц с миниатюрами
- `ProgressDialog` - диалог прогресса загрузки миниатюр
**Основные функции:**
- **Визуальный выбор страниц** - миниатюры всех страниц PDF с возможностью клика
- **Ручной ввод диапазонов** - поле для ввода диапазонов вида "1-5, 7, 10-15"
- **Автоматическая генерация миниатюр** - с использованием PyMuPDF и Pillow
- **Умное позиционирование** - окно не перекрывает панель задач Windows
- **Двусторонняя синхронизация** - связь между визуальным выбором и текстовым вводом
- **Статистика выбора** - отображение количества выбранных страниц
**Основные методы:**
- `show()` - отображение диалога и возврат выбранных страниц
- `_load_thumbnails()` - загрузка и генерация миниатюр страниц
- `_parse_page_range()` - парсинг текстовых диапазонов страниц
- `_update_thumbnails_display()` - обновление отображения миниатюр
**Особенности интерфейса:**
- Миниатюры с цветовой индикацией типа контента (таблицы/чертежи/приложения)
- Поле для ручного ввода с валидацией формата
- Кнопки быстрого выбора ("Выбрать все", "Очистить все")
- Прогресс-бар загрузки миниатюр для больших PDF
- Автоматическая прокрутка колесиком мыши
**Использование:**
```python
selector = PdfPageSelector(parent=root, total_pages=50, pdf_path="document.pdf")
selected_pages = selector.show()  # Возвращает список страниц или None

🛠️ spec_generator.py - ГЕНЕРАТОР СПЕЦИФИКАЦИЙ
Назначение: Формирование итоговых спецификаций и экспорт в различные форматы
Основные функции:

Подготовка данных для предпросмотра

Расчет мощностей, весов и объемов

Экспорт в Excel и CSV форматы

Форматирование и оформление выходных документов

🐛 debug_tools.py - ИНСТРУМЕНТЫ ОТЛАДКИ
Назначение: Диагностика и отладка работы программы
Основные функции:

Логирование работы парсеров

Отображение шаблонов распознавания

Анализ результатов подбора

### Файлы данных

#### 📋 `Матрица.xlsx` - БАЗА ДАННЫХ RADIATOR
**Структура:**
- Листы по типам подключения: "VK-правое 10", "K-боковое 22", etc.
- Колонки: Артикул, Наименование, Вес, Объем, Мощность
- Лист "Кронштейны" - артикулы креплений

#### 🎭 `patterns.json` - БАЗА ЗНАНИЙ
**Формат:** JSON с шаблонами распознавания
**Содержит:**
- Регулярные выражения для названий
- Параметры: подключение, тип, высота, длина
- Примеры обучения
- Приоритеты совпадений

#### 📖 `inst.pdf` - ИНСТРУКЦИЯ
**Назначение:** Руководство пользователя

#### 💰 `Прайс-лист.xlsx` - ЦЕНЫ
**Назначение:** Актуальные цены на продукцию

## Поток данных

1. **Загрузка спецификации** → `pdf_parser.py` / другие загрузчики
2. **Распознавание названий** → `parsers.py` 
3. **Поиск шаблонов** → `pattern_manager.py`
4. **Сопоставление аналогов** → `correspondence_manager.py`
5. **Выбор аналогов** → `meteor_selector.py`
6. **Перенос в матрицу** → `interface_builder.py`
7. **Генерация спецификации** → основной файл

## Зависимости между модулями